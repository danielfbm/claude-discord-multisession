{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "AskUserQuestion",
        "hooks": [
          {
            "type": "command",
            "command": "bun \"${CLAUDE_PLUGIN_ROOT}/server.ts\" --ask-hook",
            "timeout": 700
          }
        ]
      }
    ]
  }
}
